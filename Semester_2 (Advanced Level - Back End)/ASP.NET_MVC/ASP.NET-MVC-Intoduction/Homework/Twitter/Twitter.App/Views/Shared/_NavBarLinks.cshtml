@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    @Html.ActionLink("Hello " + User.Identity.Name + " !", "Index", "Users", routeValues: new { username = User.Identity.Name }, htmlAttributes: null)
}

